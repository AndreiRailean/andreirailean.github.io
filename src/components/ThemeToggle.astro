---
import { DarkModeToggle } from "@/components/DarkModeToggle"

// The control only. Deciding what the document's scheme *is* belongs to
// `ThemeScript.astro`, which runs in `<head>`; this used to carry that script
// too, and having the class set from two places at two different moments is what
// made "System" stop working after the first visit.
---

<DarkModeToggle client:load />
